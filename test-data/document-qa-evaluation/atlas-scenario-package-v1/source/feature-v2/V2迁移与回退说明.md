# V2 迁移与回退说明

V2 当前只用于演示验证。若增量结果不符合预期，项目管理员可以使用导入前自动生成的场景修订号发起一次业务回退。回退会恢复场景、节点、连线和模板事实，然后创建新的拓扑刷新任务。

业务回退不删除原导入审计记录，也不复用原刷新任务。dry-run 因为没有写入数据，所以没有可回退的修订号。

V2 文档和 PR 材料不能证明 main 已具备相同能力；切换到 main 后应按照包版本 1.3、20 MiB 和全量替换规则操作。

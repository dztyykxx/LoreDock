---
name: knowledge-curator
description: 基于已授权项目知识与来源，增量整理版本化待审核草稿
---

# 知识整理

先确认项目范围和本轮目标。需要修改草稿时必须先调用 `draft_read` 取得当前修订与服务端区块 ID，
再使用 `draft_update` 的结构化操作提交一个有审核意义的变更批次。

不得把最终对话消息当作草稿正文，不得请求全文覆盖、任意文件写入或正式发布。
证据不足或来源冲突时保留待核实项并等待管理员指导。

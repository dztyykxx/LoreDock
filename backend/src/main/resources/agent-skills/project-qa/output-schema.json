{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "project-qa-v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["text", "resultType", "answerBasis", "citations", "refusalReason", "sourceConflict"],
  "properties": {
    "text": {"type": "string", "minLength": 1, "maxLength": 12000},
    "resultType": {"type": "string", "enum": ["ANSWER", "REFUSAL"]},
    "answerBasis": {"type": ["string", "null"], "enum": [null, "BUSINESS_RULE"]},
    "citations": {
      "type": "array",
      "maxItems": 20,
      "uniqueItems": true,
      "items": {"type": "string", "pattern": "^[0-9]+$"}
    },
    "refusalReason": {
      "type": ["string", "null"],
      "enum": [null, "INSUFFICIENT_EVIDENCE", "OUT_OF_SCOPE", "SOURCE_CONFLICT", "AGENT_CITATION_INVALID", "OUTPUT_POLICY_VIOLATION"]
    },
    "sourceConflict": {"type": "boolean"}
  }
}

# 场景包导入与导出指南

LoreDock 的场景包使用 ZIP 容器，知识正文只接受 Markdown 与纯文本。导入前先选择通用、项目或分支范围，系统逐项校验文件类型、大小与压缩比，再为成功条目创建知识草稿；草稿必须经人工审核发布后才能进入检索。

导出包包含清单、已选知识材料、需求说明、关联提交摘要和测试证据索引。导出不会包含登录口令、访问凭据、服务端对象键或本地绝对路径。接收方应使用清单中的稳定标识核对材料，不应把导出结果视为自动发布的正式知识。

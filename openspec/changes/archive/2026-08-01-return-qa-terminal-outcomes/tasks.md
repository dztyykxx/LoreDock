## 1. 契约与失败测试

> 协同说明：本 change 的终态、SSE、引用/拒答行为作为 `simplify-backend-mvc-runtime` 的兼容基线；后续包结构、接口、Agent Runtime、表结构和主键重构由该 change 接管，不在此继续固化旧分层。任务 4.4 的真实模型验证仍须独立完成。

- [x] 1.1 先扩展后端 `WebQaQuestionResponse` 与前端 `QaQuestion` 契约，加入可空 `failureMessage`，并编写带中文业务说明的 HTTP 映射失败测试，证明完成结果不携带失败文案、失败/终止结果必须携带脱敏文案。
- [x] 1.2 为 Agent 运行适配器编写失败测试：成功空检索后模型持续请求工具时应形成 `REFUSAL/INSUFFICIENT_EVIDENCE`；已有合格证据、工具异常、版本变化或模型异常时不得被转换为拒答，并输出与断言一致的步骤、模型调用、证据和终态日志。
- [x] 1.3 为 Skill 引导编写失败测试，证明 `project_qa` 1.0.1 成为新运行启用版本、1.0.0 仍可读取、同版本内容冲突仍被拒绝。
- [x] 1.4 为 Vue 回答面板与终态刷新编写失败测试，覆盖拒答正文、步骤上限的人类可读说明、错误码次要显示、失败时不显示部分文本以及主动重试生成新幂等键。

## 2. 无证据确定性拒答

- [x] 2.1 扩展执行台账记录成功检索调用数与合格证据数，在中文注释中说明“成功空检索”和“检索故障”必须分离的业务原因，并补充结构化生产日志字段。
- [x] 2.2 在步骤或模型调用预算阻止下一次调用时，仅对“至少一次检索成功、零合格证据、无其他执行错误”生成模板化 `REFUSAL/INSUFFICIENT_EVIDENCE`，复用现有结果校验、完成事务和拒答事件路径。
- [x] 2.3 保持已有证据后的预算耗尽、超时、模型/工具/权限/版本错误为原失败或终止事实，运行相关测试并确认未发布回答增量或伪助手消息。
- [x] 2.4 将内置 `project_qa` Skill 升级为 1.0.1，加入快照可用性、空结果停止和无快照拒答规则；把 `codeSnapshotAvailable` 加入脱敏固定范围提示并通过 Skill 校验与引导测试。

## 3. Web 终态说明

- [x] 3.1 实现服务端 `AgentErrorCode → failureMessage` 穷尽白名单映射和未知错误安全兜底，确保不读取异常原文、内部端点、路径、隐藏提示或证据正文，并为关键映射输出可核验测试日志。
- [x] 3.2 将 `failureMessage` 接入问答创建、历史和详情的统一安全快照，保持 SSE 只发送稳定错误码并在终态后通过详情收敛；验证新增字段对旧客户端为可空兼容扩展。
- [x] 3.3 调整回答面板：完成拒答在回答主区域显示 `resultText`、原因和知识缺口入口；失败/终止显示服务端 `failureMessage`、操作建议和次要错误码，同时保持失败徽标且不展示来源依据标签。
- [x] 3.4 调整终态刷新逻辑，在失败或终止时清空/忽略本地 `partialText`，确认刷新、断线重连和切换历史记录都以服务端快照为准且不会创建新运行。

## 4. 集成验证与交付

- [x] 4.1 运行 Agent、Skill、Web QA 应用层与 Web 层相关单元测试，确认每个新增测试都有中文业务目的注释，生产日志和测试证据能区分拒答与运行故障。
- [x] 4.2 使用真实 PostgreSQL 运行相关持久化与 SSE 集成测试，验证完成拒答会投影唯一助手消息、失败/终止不会投影助手消息，且无需新增 Flyway 迁移。
- [x] 4.3 运行前端类型检查、组件测试和构建，验证拒答、步骤上限、模型不可用、刷新恢复和重试入口的实际渲染结果。
- [ ] 4.4 真实模型最小 Smoke（`COMPLETED/REFUSAL`、`INSUFFICIENT_EVIDENCE`、步骤数、页面文案、模型故障仍显示失败而非拒答）已随 `后端第一阶段收口与模块化重构任务` 的阶段七最终验收转入新 Change，不在本 Change 重复执行。
- [x] 4.5 严格校验 OpenSpec change，检查工作区差异、敏感信息与受影响文档；本 Change 的规格增量已同步到主规格，剩余验收转移说明已记录，按流程归档本 Change。

## 1. 冻结依赖与契约

- [x] 1.1 通过 Sa-Token 官方发布/仓库、Spring Boot 依赖管理、Vue Router 官方文档和字体官方许可证核验 Spring Boot 4 starter、`spring-security-crypto`、Vue Router、Geist/Geist Mono 的精确版本、维护状态、许可证与传递依赖；先完成最小解析/启动/构建验证，再锁定 Maven、npm 锁文件和自托管字体资产，不引入功能重叠框架。
- [x] 1.2 先定义身份能力的 `WebRole`、固定账号目录、登录/会话用例、MCP Token 校验端口、请求/响应模型和新增稳定错误码，为公共接口及失败语义补充中文 Javadoc；此步不实现 Sa-Token、BCrypt 校验或 Servlet 过滤器。
- [x] 1.3 先定义项目/分支领域输入、command/query 用例、仓储端口、应用 DTO 以及普通/管理员 HTTP 契约，明确字段边界、默认 `main`、401/403/404/409、幂等性和兼容策略并补充中文 Javadoc；此步不创建 Mapper、Controller 或业务实现。

## 2. Web 身份、会话与授权

- [x] 2.1 为固定账号配置和登录用例编写带中文业务目的注释的失败测试，覆盖管理员/成员成功登录、未知账号与错误密码相同失败外观、缺失/无效 BCrypt 配置拒绝就绪、重复账号拒绝和敏感值不泄露；逐项运行并确认因目标行为缺失而失败。
- [x] 2.2 实现强类型固定账号配置、`BCryptPasswordEncoder` 校验和最小登录应用服务，使 2.1 通过；检查实现类中文注释说明固定账号边界、统一失败外观和不自行实现密码哈希的原因。
- [x] 2.3 为 `/api/auth/login|session|logout`、受保护 `/api/**`、管理员 `/api/admin/**` 和 Cookie 属性编写带中文业务目的注释的失败 Web 测试，覆盖会话恢复、幂等退出、未登录 401、成员写入 403、管理员允许、响应不含 Token/哈希及重启会话失效边界。
- [x] 2.4 接入 Sa-Token Spring Boot 4 starter，实现 Auth Controller、同站 HttpOnly 会话 Cookie、路由拦截、角色解析和统一 `ApiError` 映射，使 2.3 通过；不得并存 Spring Security 认证链，并为 401/403 区分和客户端角色不可信规则补充中文原因注释。
- [x] 2.5 为审计操作者切换编写带中文业务目的注释的失败测试，覆盖登录管理员写操作记录账号标识、无 Web 身份的后台/启动工作仍记录 `SYSTEM`，确认测试因当前固定 `SYSTEM` 实现而失败。
- [x] 2.6 用认证感知 `ActorProvider` 替换 T1 默认实现并使 2.5 通过；在测试保护下检查请求上下文清理和机器工作不得冒充人工操作的中文注释，确认现有后台任务与对象存储测试不回归。

## 3. 独立 MCP Token 边界

- [x] 3.1 为 MCP Token 配置与 `/mcp/**` 前置过滤编写带中文业务目的注释的失败测试，覆盖缺失/畸形/错误 Token 的 401、管理员 Web 会话不可替代、有效 Token 继续过滤链、机器身份不能访问管理 API、无原值/摘要日志以及配置格式校验。
- [x] 3.2 实现只保存 SHA-256 摘要的强类型配置、严格 Bearer 解析、常量时间比较、`/mcp/**` 过滤器和共享安全错误写出器，使 3.1 通过；用中文注释解释高熵 Token 与人类密码采用不同哈希策略、校验先于 MCP 分派及 Web/MCP 身份隔离。
- [x] 3.3 在测试保护下统一 Sa-Token 异常、MCP 过滤器和既有 `GlobalExceptionHandler` 的错误体/trace ID/脱敏逻辑，确认错误响应和结构化日志不包含账号提交密码、Cookie、Authorization、Token 摘要或内部正文。

## 4. 项目与分支领域及数据库

- [x] 4.1 为 `ProjectIdentifier`、`BranchName`、`ProjectStatus`、默认 `main` 和分支选择编写带中文业务目的注释的失败领域测试，覆盖长度/字符边界、合法 `feature/import-export`、危险路径样式、停用不删除和未知分支绝不回退；确认失败原因是领域规则尚未实现。
- [x] 4.2 实现最小项目/分支领域模型和值对象使 4.1 通过，并在测试保护下集中规范化与校验逻辑；为默认分支、大小写保留、分支名不得直接作为文件路径等关键规则补充中文原因注释。
- [x] 4.3 先扩展真实 PostgreSQL 迁移和映射失败测试，覆盖从 T1 V1 升级、空库/重复迁移、项目状态检查、项目标识唯一、项目内分支唯一/跨项目可重名、外键、UTC 审计以及每个 MyBatis-Plus 实体字段显式映射；运行并确认因 V2 与实体缺失而失败。
- [x] 4.4 追加 `V2__create_project_and_branch_tables.sql`，创建带命名约束和索引的 `project_space`/`project_branch`，再实现独立 Lombok 持久化实体与 BaseMapper，使 4.3 通过；不得修改 V1、自动建表、使用 H2 或 XML Mapper。

## 5. 项目应用服务、持久化与 HTTP API

- [x] 5.1 为项目仓储和应用用例编写带中文业务目的注释的失败 PostgreSQL 集成测试，覆盖项目与 `main` 原子创建、重复/并发项目标识冲突、分支创建与并发冲突、启停幂等、普通查询在数据库范围排除停用项目、管理查询保留停用项目和重启后往返。
- [x] 5.2 使用 MyBatis-Plus Java API 优先实现仓储适配器和事务化项目应用服务，使 5.1 通过；仅按命名唯一约束映射 409，其他数据库异常保留内部语义，并为事务回滚、范围前置过滤和并发约束补充中文原因注释。
- [x] 5.3 为普通 `/api/projects/**` 与管理员 `/api/admin/projects/**` 编写带中文业务目的注释的失败 Web 契约测试，覆盖全部请求/响应字段、默认/指定/未知分支、400/401/403/404/409、成员绕过前端写入无副作用、停用项目普通入口不可枚举及状态 PATCH 幂等。
- [x] 5.4 实现只调用应用用例的普通与管理员 Controller、Bean Validation 请求模型和响应映射，使 5.3 通过；为公共 API 与关键失败语义补充中文 Javadoc，不在 Controller 复制状态、分支或权限规则。
- [x] 5.5 为显式演示数据准备编写带中文业务目的注释的失败集成测试，覆盖首次创建两个项目/演示分支、重复执行复用数据、默认生产启动不准备数据和无真实内部内容/凭据。
- [x] 5.6 实现 `demo`/`test` profile 与显式开关双重控制的数据准备器，使 5.5 通过；准备器复用项目应用服务并报告 `created/reused`，不得提供生产 HTTP 写入口或把演示记录写进 Flyway。
- [x] 5.7 在测试保护下按 `project/domain|application|infrastructure` 整理依赖方向，检查领域/应用层不依赖 Controller、Mapper、持久化实体或文件路径，并复核公共接口、实现策略、事务和范围规则的中文注释。

## 6. 前端路由、会话与设计组件

- [x] 6.1 重新通过 Pencil MCP 读取 `01 · 登录`、`02 · 项目列表`、`10 · 项目设置 / 分支管理` 及其中实际使用的可复用组件/实例覆盖，记录组件数量、准确文案、图标、间距、颜色、字体和桌面尺寸；确认设计稿无破损后，再建立与当前 Vue 工程一致的实现映射。
- [x] 6.2 先定义 TypeScript 会话、项目、分支和 `ApiError` 模型及服务函数签名，再编写带中文业务目的注释的失败测试，覆盖 Cookie 请求、会话恢复、401 清理、登录后安全 redirect、管理员路由守卫和成员路由降级。
- [x] 6.3 实现最小 API 客户端、`useSession`、Vue Router 和路由守卫使 6.2 通过；拒绝外部/循环 redirect，不新增 Pinia 或通用网络框架，并检查错误分支的中文原因注释。
- [x] 6.4 为 T2 实际复用的按钮、字段、徽标、提示、页面标题、侧栏、顶部栏、项目 Hero/Tabs/Card 编写带中文业务目的注释的最小组件测试，覆盖变体、禁用/忙碌、可访问标签、角色可见性、真实 API 字段不被样例覆盖，以及无接口的设计样例不触发后端请求。
- [x] 6.5 按 Pencil 令牌实现自托管 Geist/Geist Mono、全局 CSS 变量和 6.4 的 Vue 组件，使测试通过；只实现三张 T2 页面实际使用的组件，并核对字体许可证文件、焦点样式、对比度和无运行时外网字体请求。

## 7. 登录、项目列表与项目设置页面

- [x] 7.1 为登录页编写带中文业务目的注释的失败页面测试，覆盖设计文案/字段、提交忙碌防重复、成功跳转、统一错误提示、保留账号清空密码、会话恢复和键盘/辅助技术关联。
- [x] 7.2 参考 Pencil `01 · 登录` 实现真实 API 驱动的 `LoginView`，使 7.1 通过；完成品牌侧栏、权限提示、加载和安全失败状态，不使用静态登录结果。
- [x] 7.3 为项目列表页编写带中文业务目的注释的失败页面测试，覆盖加载、名称/标识本地筛选、通用业务知识入口、空/失败/重试、管理员创建项目、成员隐藏创建入口、停用项目不出现、项目与分支字段使用真实响应，以及无接口的知识/快照样例仅作静态展示。
- [x] 7.4 参考 Pencil `02 · 项目列表` 实现 `ProjectListView`、创建项目交互和项目卡片，使 7.3 通过；项目主数据从普通 API 获取，创建成功后以服务端结果刷新，无接口的后续能力样例集中定义且不进入 API 模型或请求，错误不暴露内部异常。
- [x] 7.5 为项目设置页编写带中文业务目的注释的失败页面测试，覆盖管理员管理详情、成员只读详情、默认/切换分支、添加分支、停用确认、重复状态操作、API 失败恢复、真实接口字段优先，以及未来能力样例静态展示且不发起不存在的请求。
- [x] 7.6 参考 Pencil `10 · 项目设置 / 分支管理` 实现 `ProjectSettingsView`，使 7.5 通过；T2 基本信息只读，写控件由角色与请求状态共同控制，任何页面限制都不能替代后端授权。
- [x] 7.7 完成三张页面在电脑浏览器和 Pencil 桌面尺寸下的布局、键盘焦点顺序与可访问性；补充有业务意义的桌面交互测试，并检查关键交互注释说明权限边界、真实接口数据与静态设计样例的来源差异。本变更不实现移动端、平板或窄屏专项布局。

## 8. 全栈验证与状态同步

- [x] 8.1 运行后端单元测试、真实 PostgreSQL 集成测试、前端 Vitest、TypeScript 检查和生产构建；逐项修复失败并检查每个测试方法都有说明业务目的和防回归原因的中文注释。
- [x] 8.2 启动完整本地栈，以管理员、成员和无效身份分别验证登录、项目创建、列表、分支、启停、401/403、错误密码与 MCP Token 边界；确认数据库重启后项目/分支持久而应用重启后 Web 会话按设计失效。
- [x] 8.3 使用电脑浏览器在 Pencil 设计稿桌面尺寸逐页截图，对照三个 frame 检查组件数量、文案、字体、颜色、间距、对齐、加载/空/错误/权限状态、真实数据与静态样例边界及控制台错误；直接修正实现并重新验证。
- [x] 8.4 更新 `.env.example`、README/运行与故障排查说明，记录账号 BCrypt 哈希、MCP Token 摘要生成、同站 Cookie、演示数据开关、接口入口和单实例会话限制；示例不得包含可用凭据、内部地址或真实业务材料。
- [x] 8.5 对照两份增量规格逐条复核实现和验证证据，勾选完成任务并运行 `openspec validate add-auth-project-branch-management --strict`；全部门禁满足后把 MVP 开发计划 T2 更新为完成并准备同步主规格与归档 change。

## 1. 冻结依赖与契约

- [x] 1.1 通过 Apache Commons Compress 官方发布说明、安全页、许可证文件和 Maven 依赖树重新核验 1.28.0 的维护状态、Java 21/Spring Boot 4 兼容性、传递依赖与已知安全风险；完成最小解析测试后锁定依赖与强类型导入上限配置，不引入 Markdown 渲染器、文档转换器或第二套压缩库。
- [x] 1.2 先定义 `KnowledgeDocument` 所需的格式、范围、来源、状态、目录、标签、替代与修订契约，以及创建、编辑、发布、归档、普通浏览和管理查询的应用端口/DTO；公共接口与失败语义补充中文 Javadoc，此步不创建 Mapper、Controller 或业务实现。
- [x] 1.3 先定义普通 `/api/knowledge-documents/**`、管理 `/api/admin/knowledge-documents/**` 的请求/响应模型、分页模型和稳定错误码，冻结字段、明确上下文、400/401/403/404/409、排序、幂等性与兼容策略；此步不实现 Controller。
- [x] 1.4 先定义导入批次/条目、ZIP 检查、对象补偿、`PublishedKnowledgeIndexReader`、generation 重建和 `KNOWLEDGE_REINDEX` 任务端口，以及 multipart/任务 HTTP 模型与 413/415/422/404 语义；为不可信输入、single-flight 和失败回退边界补充中文 Javadoc，此步不实现解析器或任务处理器。

## 2. 文档领域规则

- [x] 2.1 为标题/正文/目录/标签/来源和 `GLOBAL|PROJECT|BRANCH` 组合编写带中文业务目的注释的失败领域测试，覆盖 Unicode/长度/重复标签、非法逻辑路径、Wiki/上传来源必填项、范围残留/缺失和分支归属输入；逐项运行并确认因规则尚未实现而失败。
- [x] 2.2 实现最小文档值对象、范围与来源模型使 2.1 通过；在测试保护下集中规范化与不变量，正文保持纯文本，并用中文注释解释逻辑目录不是文件路径、范围 UUID 不得由名称拼接的原因。
- [x] 2.3 为创建草稿、同值编辑幂等、有效编辑增加 revision、首次发布、重复发布、草稿/已发布归档、归档终态和发布审计编写带中文业务目的注释的失败状态机测试。
- [x] 2.4 实现 `KnowledgeDocument` 聚合的最小创建、编辑、发布与归档行为使 2.3 通过；不增加版本历史或自动索引，并检查终态、修订和重复请求规则的中文原因注释。
- [x] 2.5 为自替代、不同范围、循环链、旧文档非发布态、一个旧文档被竞争替代和成功替代发布编写带中文业务目的注释的失败领域/应用边界测试。
- [x] 2.6 实现替代关系校验与发布计划使 2.5 通过；在测试保护下重构领域依赖，确保领域层不依赖 Spring、项目应用端口、数据库、文件系统或 HTTP。

## 3. V3 迁移与持久化基础

- [x] 3.1 先扩展真实 PostgreSQL 迁移和显式映射失败测试，覆盖从现有 V1/V2 升级、空库/重复迁移、文档范围/状态/来源/revision 检查、项目/分支/对象/任务外键、替代唯一与非自指、导入计数、generation 唯一活动约束以及所有实体逐字段 MyBatis-Plus 映射。
- [x] 3.2 追加 `V3__create_knowledge_document_tables.sql`，创建文档、标签、导入批次/条目和索引 generation/投影表及命名约束/索引，再实现独立 Lombok 持久化实体与 BaseMapper，使 3.1 通过；不得修改 V1/V2、自动建表、使用 H2 或 XML Mapper。
- [x] 3.3 为文档仓储编写带中文业务目的注释的失败 PostgreSQL 集成测试，覆盖聚合往返、标签替换、同值不更新、revision 条件更新、管理员状态/范围/目录/标签分页，以及普通全局查询只含 `GLOBAL/PUBLISHED`。
- [x] 3.4 扩展 3.3 的失败测试覆盖项目查询只含通用 + 当前项目 + 当前分支、其他项目/分支不加载、草稿/归档不加载、直接读取上下文不匹配统一不可见和稳定分页排序。
- [x] 3.5 使用 MyBatis-Plus Java API优先实现文档/标签仓储、复杂范围查询和映射，使 3.3～3.4 通过；只在 Java API 无法清楚表达的范围联合/锁行查询使用注解 SQL，并为查询前置隔离和条件更新补充中文原因注释。
- [x] 3.6 在测试保护下检查 `knowledge/domain|application|infrastructure` 依赖方向和实体/领域/DTO 分离，补齐公共类、公共方法、事务边界与唯一约束映射的中文注释。

## 4. 范围解析与文档应用服务

- [x] 4.1 为 `KnowledgeScopeResolver` 编写带中文业务目的注释的失败测试，覆盖普通入口只解析已启用项目、分支为空默认 `main`、未知分支绝不回退、管理入口可解析停用项目、分支必须属于项目以及 `GLOBAL` 不访问项目端口。
- [x] 4.2 实现复用 T2 `ProjectQueryUseCase`/`AdminProjectQueryUseCase` 的范围适配器使 4.1 通过；知识应用层只依赖自身端口，不复制项目状态、默认分支或分支名规则。
- [x] 4.3 为创建/编辑应用用例编写带中文业务目的注释的失败 PostgreSQL 集成测试，覆盖 Markdown/纯文本草稿、审计、所有元数据、原子保存、同值幂等、归档编辑冲突、范围在保存前解析和数据库失败不留部分标签/来源。
- [x] 4.4 实现事务化创建/编辑服务使 4.3 通过；只接受已解析范围和值对象，保留业务异常上下文，并为已发布编辑保持发布但索引变 stale 的规则补充中文原因注释。
- [x] 4.5 为普通/管理员查询用例编写带中文业务目的注释的失败集成测试，覆盖目录节点、分页摘要、详情字段、停用项目普通 404、跨范围 ID 统一 404、管理员筛选及替代/同步信息。
- [x] 4.6 实现普通/管理员查询服务使 4.5 通过，确认 Controller 未来只做传输映射且任何入口都不能先跨范围加载再隐藏。
- [x] 4.7 为发布、归档和替代发布编写带中文业务目的注释的失败 PostgreSQL 集成测试，覆盖目标状态幂等、按 UUID 稳定锁序、同事务归档旧文档、循环/不同范围/并发竞争 409、任一步失败全回滚和归档立即失去实时资格。
- [x] 4.8 实现事务化生命周期服务和命名唯一约束映射使 4.7 通过；其他数据库异常不得误报业务冲突，并检查双层实时资格过滤的中文原因注释。

## 5. 文档 HTTP API

- [x] 5.1 为普通 `GET /api/knowledge-documents` 与 `/{id}` 编写带中文业务目的注释的失败 Web 契约测试，覆盖 GLOBAL/PROJECT 明确上下文、默认/指定分支、目录分页、完整响应、400/401/404、跨项目/分支/状态不泄露和停用项目不可见。
- [x] 5.2 实现只调用普通查询用例的 `KnowledgeDocumentController` 与 HTTP 映射使 5.1 通过；公共 API 使用中文 Javadoc，不在 Controller 复制范围、状态或项目启停规则。
- [x] 5.3 为管理员列表/详情/创建/编辑端点编写带中文业务目的注释的失败 Web 契约测试，覆盖全部字段、筛选/分页、201、同值 PUT、400/401/403/404/409、成员直接写入无副作用及错误响应不含正文/内部异常。
- [x] 5.4 实现管理员文档 Controller、Bean Validation 和响应映射使 5.3 通过；前端字段或角色状态不得成为服务端授权与校验依据。
- [x] 5.5 为发布、归档和带 `replacesDocumentId` 发布端点编写带中文业务目的注释的失败 Web 测试，覆盖确认后的真实状态、重复请求幂等、替代追溯、成员 403、状态/替代冲突 409 和回滚后的响应一致性。
- [x] 5.6 实现生命周期端点使 5.5 通过；在测试保护下统一新增业务异常与 `ApiError`/trace ID/脱敏日志，确保归档或替代失败不被显示为成功。

## 6. 安全文档导入

- [x] 6.1 为强类型导入配置、外层扩展名/签名、严格 UTF-8/BOM 和标题/目录派生编写带中文业务目的注释的失败测试，覆盖配置非法拒绝就绪、Markdown/文本成功、非法编码失败、415 类型拒绝和 413 上传上限。
- [x] 6.2 实现计数字节上传、类型识别、严格文本解码和单文件候选解析使 6.1 通过；不使用平台默认编码、不解析 front matter、不渲染 HTML，并检查不可信正文规则的中文注释。
- [x] 6.3 使用程序化小型 fixture 为 ZIP 中央目录检查编写带中文业务目的注释的失败测试，覆盖安全多级目录、绝对/盘符/`..`/NUL、Unix 符号链接/非普通文件、重复规范化路径、加密/分卷/损坏、条目/单项/累计/压缩比上限和临时文件清理。
- [x] 6.4 使用 Commons Compress `ZipFile` 和服务生成的单一临时文件实现两阶段 ZIP 检查使 6.3 通过；不得按条目名写文件或跟随链接，批次级异常必须发生在文档事务前且错误信息脱敏。
- [x] 6.5 为导入批次应用服务编写带中文业务目的注释的失败 PostgreSQL/对象存储集成测试，覆盖单文件成功/失败项、ZIP 成功/失败/忽略计数、部分成功、只有忽略项、稳定明细排序、每项独立事务、范围失效、重复上传新建草稿和所有成功项保持 `DRAFT`。
- [x] 6.6 扩展 6.5 的失败测试覆盖对象成功但批次记录失败的幂等补偿删除、批次级 422 不留文档、条目保存中途失败不留孤立标签/关系、补偿失败只留不可查询对象和日志不含对象键/正文。
- [x] 6.7 实现导入批次/条目仓储、每项事务服务和对象存储协调使 6.5～6.6 通过；复用文档创建与范围规则，不自动发布、不自动索引且不调用模型。
- [x] 6.8 为 multipart 导入和批次查询编写带中文业务目的注释的失败 Web 契约测试，覆盖 201 结果、413/415/422、普通成员 403 且不接收正文、历史查询 404、响应不含对象键和文件名/解析器错误安全转义。
- [x] 6.9 实现导入 Controller 与响应映射使 6.8 通过；在测试保护下复核 Commons Compress 只按 ZIP 最小功能引入、临时文件 finally 清理和安全分支中文原因注释。

## 7. generation 索引与后台任务

- [x] 7.1 先为 `BackgroundJobService.submitSingleFlight` 和按类型活动任务查询编写带中文业务目的注释的失败单元/真实 PostgreSQL 测试，覆盖同类型 PENDING/RUNNING 复用 ID、终态后可新建、默认 `submit` 仍非幂等、先持久化再执行和不同任务类型互不阻塞。
- [x] 7.2 在 T1 任务能力内实现单实例 JVM 锁、活动任务仓储查询和最小接口扩展使 7.1 通过；不改变既有状态机、恢复或默认提交语义，并用中文注释标明 single-flight 仅适用于单实例 MVP。
- [x] 7.3 为索引 generation 仓储和同步状态编写带中文业务目的注释的失败 PostgreSQL 集成测试，覆盖无投影、发布待同步、revision 相等、已发布编辑 stale、草稿不适用、最多一个 ACTIVE、活动批量读取和项目/分支参数过滤。
- [x] 7.4 实现 generation/投影仓储、`PublishedKnowledgeIndexReader` 与派生同步状态使 7.3 通过；不回写 `indexed_revision`，并为活动投影仍需实时资格复核补充中文原因注释。
- [x] 7.5 为 `KNOWLEDGE_REINDEX` 编写带中文业务目的注释的失败集成测试，覆盖只快照 PUBLISHED、REPEATABLE READ 一致性、成功原子切换、旧 generation 退休、失败/进程中断保留旧 ACTIVE、未完成投影不可见和旧退休数据尽力清理失败不影响活动查询。
- [x] 7.6 实现重建应用服务与 `JobHandler` 使 7.5 通过；处理器只做 PostgreSQL 本地投影，不引入 Lucene/Embedding/pgvector 写入，不吞异常，并更新有意义的进度与心跳。
- [x] 7.7 为重新索引提交/查询端点编写带中文业务目的注释的失败 Web 测试，覆盖 202、并发重复返回同一 ID、PENDING/RUNNING/SUCCEEDED/FAILED 视图、非知识任务 ID 404、成员 403 和脱敏失败摘要。
- [x] 7.8 实现知识索引任务 Controller 使 7.7 通过，并用集成测试确认归档文档即使存在于旧 ACTIVE 投影也会被实时资格端口排除、失败重建不影响普通浏览。

## 8. 前端 API、路由与设计组件

- [x] 8.1 重新通过 Pencil MCP 读取 `03 · 项目详情 / 知识管理`、`05 · 新建 / 编辑知识` 及实际组件实例/覆盖，记录准确文案、桌面尺寸、目录层级、编辑主次区域、表单、状态和按钮；确认设计稿无破损后建立 Vue 映射，不用普通工具读取 `.pen`。
- [x] 8.2 先定义 TypeScript 文档、分页、导入、索引任务与 `ApiError` 模型及服务函数签名，再编写带中文业务目的注释的失败 API 测试，覆盖查询参数编码、multipart JSON options、Cookie、错误映射、正文/文件名只作文本和轮询终态。
- [x] 8.3 实现最小知识 API 客户端使 8.2 通过；复用现有 HTTP 客户端，不新增状态框架、通用请求封装或前端 Markdown 依赖。
- [x] 8.4 为 `/knowledge`、项目知识、详情、新建/导入、编辑路由和 `ProjectTabs`/侧栏/项目卡片导航编写带中文业务目的注释的失败测试，覆盖会话、管理员路由、成员降级、`main` 默认分支、设置路由保留和安全 redirect。
- [x] 8.5 实现路由与现有导航组件更新使 8.4 通过；`/projects/:identifier` 进入真实知识页，管理员设置继续使用现有 settings 路由，不再以禁用静态 tab 代替导航。
- [x] 8.6 为 `DocumentDirectoryTree`、`DocumentList`、`DocumentStatusBadge`、`ScopeFields`、`TagInput`、`ImportResultPanel` 和 `ConfirmDialog` 编写带中文业务目的注释的最小组件测试，覆盖状态、键盘/标签、忙碌禁用、危险确认、三类导入结果和不可信文本不执行 HTML。
- [x] 8.7 按现有 Pencil 令牌实现 8.6 的最小复用组件使测试通过；只抽取真实重复边界，复用 AppButton/Notice/ProjectHero/Tabs，不引入多余组件库或运行时外网资产。

## 9. 知识目录、编辑、导入与重建页面

- [x] 9.1 为通用/项目知识目录与详情页编写带中文业务目的注释的失败页面测试，覆盖加载、空、失败/重试、目录/分页、分支切换不串数据、跨范围 404、成员只见已发布、管理员真实状态和上下文保留。
- [x] 9.2 参考 Pencil `03` 实现 `KnowledgeWorkspaceView` 和详情区域使 9.1 通过；数据全部来自真实 API，加载前不展示设计样例，正文/来源用文本渲染且错误不清空当前上下文。
- [x] 9.3 为新建/编辑页编写带中文业务目的注释的失败页面测试，覆盖 Markdown/纯文本、三级范围联动、目录/标签/来源、保存忙碌防重复、字段错误、同值保存、归档只读、发布/归档确认和同范围替代候选。
- [x] 9.4 参考 Pencil `05` 实现 `KnowledgeEditorView` 与生命周期操作使 9.3 通过；采用原生 textarea 和右侧元数据栏，不使用 `v-html`/WYSIWYG，提交后刷新真实服务端状态并突出 `PENDING|STALE`。
- [x] 9.5 为导入流程编写带中文业务目的注释的失败页面测试，覆盖支持格式/限制说明、文件与 options、上传忙碌、成功/失败/忽略分组、部分成功文档入口、批次级 413/415/422、普通成员无入口和不可信文件名安全显示。
- [x] 9.6 实现导入面板与历史结果展示使 9.5 通过；批次级失败不得伪造条目成功，部分成功必须同时保留全部三类计数与原因。
- [x] 9.7 为重新索引入口编写带中文业务目的注释的失败页面测试，覆盖 202、重复任务复用、有限轮询、路由离开/终态停止、失败摘要、浏览继续可用和成员隐藏；实现任务状态面板使测试通过。
- [x] 9.8 完成两张页面在电脑浏览器和 Pencil 桌面尺寸下的布局、焦点顺序、表单标签、状态播报、确认对话框与错误恢复；补充有业务意义的桌面交互测试，并检查权限/不可信内容/索引状态的中文原因注释。本变更不实现移动端专项布局。

## 10. 全栈验证、文档与状态同步

- [x] 10.1 运行后端领域/应用/Web 单元测试、真实 PostgreSQL 集成测试、前端 Vitest、TypeScript 检查和生产构建；逐项修复失败并检查每个测试方法都有说明业务目的与防回归原因的中文注释。
- [x] 10.2 启动完整本地栈，以管理员、成员和未登录身份验证 Markdown/文本新建编辑、三级范围、目录浏览、发布/归档/替代、跨项目/分支隔离、停用项目、single-flight 重建失败回退以及成员直接写入 403。
- [x] 10.3 使用仅含模拟内容的正常/恶意 ZIP 验证路径穿越、符号链接、重复名、损坏/加密/分卷、压缩炸弹上限、非法 UTF-8、部分成功、对象补偿和日志脱敏；再用脱敏验收材料确认批量结果，不提交公司 Wiki 原文或凭据。
- [x] 10.4 使用电脑浏览器在 Pencil 设计稿桌面尺寸逐页截图，对照 `03`、`05` 检查组件数量、文案、字体、颜色、间距、目录层级、编辑栏、导入/索引状态、权限状态、键盘操作和控制台错误；直接修正实现并重新验证。
- [x] 10.5 更新 `.env.example`、README/运行与故障排查说明，记录导入/反向代理上限、Commons Compress 版本与许可证、支持格式、索引重建/失败回退、单实例 single-flight、原始对象与备份边界；示例不得包含可用凭据、内部地址或真实材料。
- [x] 10.6 对照两份增量规格逐条复核实现与验证证据，勾选完成任务并运行 `openspec validate add-knowledge-document-lifecycle --strict`；全部完成门禁满足后才把 MVP 开发计划 T3 更新为 `[x]`，并准备同步主规格与归档 change。

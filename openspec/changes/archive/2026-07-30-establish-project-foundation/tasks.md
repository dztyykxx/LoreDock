## 1. 冻结版本与建立工程骨架

- [x] 1.1 核验公司构建环境可获得的 GA 版本，锁定 Java 21、Spring Boot、Spring AI、Lucene、PostgreSQL/pgvector、MyBatis-Plus、Lombok、Node.js、包管理器及构建插件的精确版本，并在 README 记录版本矩阵和选择依据；确认没有动态、SNAPSHOT 或 Milestone 依赖。
- [x] 1.2 创建 `backend/` Maven Wrapper 单体工程和 `frontend/` Vue 3 + TypeScript + Vite 工程，配置 Java/Node 工具链约束、前端锁文件以及单元测试、集成测试、前端测试的独立命令，验证不兼容工具链会在编译测试前失败。
- [x] 1.3 先定义系统状态响应契约和配置属性边界，再编写带中文业务目的注释的失败测试，随后实现最小后端存活/就绪检查与前端运行状态页；确认响应不暴露环境变量、连接串或其他敏感配置。

## 2. 数据库与可重复迁移

- [x] 2.1 先配置真实 PostgreSQL/pgvector Testcontainers 测试入口并编写带中文业务目的注释的失败集成测试，覆盖空库初始化、pgvector 启用、基础表/约束存在和重复迁移不修改历史；运行并确认测试因迁移尚不存在而失败。
- [x] 2.2 编写只追加的 Flyway 初始迁移，创建 `stored_object`、`background_job`、状态/进度/时间/审计约束和必要索引，使 2.1 的迁移测试通过，并验证已执行迁移被修改时应用拒绝就绪。
- [x] 2.3 先编写带中文业务目的注释的失败测试，要求每个 MyBatis-Plus 持久化实体显式声明表、主键和全部字段映射，并禁止 XML Mapper；再建立独立于领域模型的 Lombok 实体和 Mapper，在真实 PostgreSQL 中验证 UTC 时间、状态及字段往返，确保未启用自动建表或 H2 替代验证。

## 3. 平台 API、时间、审计与日志约定

- [x] 3.1 先定义 `ApiError`、字段错误、稳定错误码、时钟端口、操作者端口和审计值对象等公共契约，添加符合规范的中文 Javadoc，不编写异常处理实现。
- [x] 3.2 编写带中文业务目的注释的失败测试，覆盖已知错误、未知异常、多个字段校验、敏感字段不回显、UTC 序列化、审计创建/更新和 trace ID 关联；逐项运行并确认因目标行为缺失而失败。
- [x] 3.3 实现最小全局异常映射、参数校验、可替换 UTC 时钟、`SYSTEM` 操作者、持久化审计辅助、trace 过滤器和结构化日志配置，使 3.2 测试通过且未知异常仍保留服务端上下文。
- [x] 3.4 在测试保护下集中脱敏与错误映射规则，检查公共类/方法和关键安全分支的中文注释，确认错误响应与日志不包含堆栈、SQL、绝对路径、Authorization、Token、密码或正文。
- [x] 3.5 先编写带中文业务目的注释的日志配置契约测试，确认默认 profile 不强制 JSON、`prod` profile 启用 Logstash JSON；确认测试因当前默认配置错误而失败后，将结构化控制台配置移入生产 profile，更新运行说明并使测试通过。

## 4. 本地持久化对象存储

- [x] 4.1 先定义 `ObjectStorage` 端口、写入元数据、返回模型和分类异常的 Java 接口/Javadoc，明确写入、读取、存在性检查及幂等删除语义，不创建本地适配实现。
- [x] 4.2 编写带中文业务目的注释的领域和集成失败测试，覆盖写入后字节/SHA-256 一致、重启读取、对象不存在、重复删除、写入中断清理、路径穿越、绝对路径、符号链接逃逸和恶意原始文件名；确认失败原因是实现缺失。
- [x] 4.3 实现流式哈希、根目录约束、临时文件原子发布、基于 MyBatis-Plus Java API 优先且必要时使用方法注解 SQL 的 `stored_object` 元数据事务及失败补偿，不使用 XML Mapper；使 4.2 测试通过，并为存储根目录不可写和空间/IO 失败保留稳定错误语义。
- [x] 4.4 在测试保护下将对象存储整理为 `domain`、`application`、`infrastructure/filesystem`、`infrastructure/persistence` 分层，拆分路径校验和文件/元数据协调逻辑；检查接口、实现策略、补偿分支与文件安全规则的中文注释，确认领域和应用代码不依赖文件系统、Mapper 或 S3/MinIO 实现。

## 5. 持久化后台任务与受控执行

- [x] 5.1 先定义任务提交、查询、进度/心跳、成功、失败、取消的应用端口和任务状态领域模型，使用中文 Javadoc 说明非幂等提交、幂等查询/取消、合法状态转换和失败语义，不实现调度器。
- [x] 5.2 编写带中文业务目的注释的失败单元测试，覆盖 `PENDING → RUNNING →` 三种终态、终态不可变、进度范围/不倒退和错误信息脱敏；运行并确认因领域规则尚未实现而失败。
- [x] 5.3 实现最小任务状态机和错误分类，使 5.2 测试通过，再在测试保护下将后台任务整理为 `domain`、`application`、`infrastructure/persistence` 分层，消除重复状态判断并检查关键规则的中文原因注释。
- [x] 5.4 编写带中文业务目的注释的失败集成测试，覆盖提交先持久化、条件认领、短事务进度/心跳、成功、工作异常失败、执行器容量拒绝、任务间失败隔离和并发终态竞争；确认因仓储与执行器尚未实现而失败。
- [x] 5.5 实现基于 MyBatis-Plus Java API 优先且必要时使用方法注解 SQL 的任务仓储，不使用 XML Mapper；完成明确上限的线程池/队列、执行边界和优雅停机，使 5.4 测试通过；验证容量拒绝将已持久化任务终结为 `FAILED/CAPACITY_EXCEEDED`，且异常不会停止其他任务。
- [x] 5.6 编写带中文业务目的注释的陈旧心跳恢复失败测试，再实现启动恢复，将超阈值 `RUNNING` 任务标为 `FAILED/PROCESS_INTERRUPTED`、保留有效心跳任务且不自动重放；检查恢复事务和不重放规则的中文注释。

## 6. 完整栈编排与验收

- [x] 6.1 创建不含真实凭据的 `.env.example`、仅包含 pgvector PostgreSQL 及后续中间件依赖的 `compose.yaml`，并提供在宿主机启动/停止 Spring Boot 与 Vite 的统一本地开发脚本；本地开发不构建前后端容器镜像。
- [x] 6.2 编写并运行本地栈冒烟验证：空环境启动 Docker 数据库与宿主机前后端后迁移完成、后端存活/就绪成功、前端可访问；停止数据库后就绪失败但存活语义正确；重启后本地对象和任务记录仍存在。
- [x] 6.3 运行后端单元测试、真实数据库集成测试、前端测试、生产构建和 Compose 配置校验，逐项记录命令与结果；检查所有测试方法均有说明业务目的和防回归原因的中文注释。
- [x] 6.4 更新根 README、初始化/故障排查说明和架构数据模型文档，记录启动、迁移、测试、版本矩阵、对象卷备份、错误/时间/审计约定及已知单实例限制，确认示例不含内部真实数据或凭据。
- [x] 6.5 对照四份增量规格逐条复核实现与验证证据，勾选已完成 OpenSpec 任务；满足全部门禁后将开发计划 T1 更新为完成，并准备同步主规格与归档 change。

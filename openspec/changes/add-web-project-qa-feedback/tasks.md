## 1. 冻结既有 Agent 契约并修正提交后调度

- [x] 1.1 为无外层事务、存在外层事务、外层回滚和队列拒绝四个场景补充失败测试；每个测试添加中文业务注释并输出实际 runId、提交状态、调度次数和终态错误码。
- [x] 1.2 先定义提交后调度协调器与独立失败终结端口的 Java 接口、输入/失败语义和中文 Javadoc，不改变 `StartProjectQaRunUseCase` 的既有外部契约。
- [x] 1.3 最小实现“最外层事务提交后调度”和 `REQUIRES_NEW` 队列拒绝终结，使 1.1 测试转绿，并为回滚不调度、迟到回调和拒绝失败分支添加中文原因注释与结构化日志。
- [x] 1.4 运行 T6A 启动幂等、运行状态机、恢复与有界执行器回归测试，确认直接调用仍只调度一次且错误码不变；只在测试保护下重构重复事务代码。

## 2. 建立 V7 数据契约与显式持久化映射

- [x] 2.1 先编写真实 PostgreSQL 迁移失败测试，覆盖 V1→V7、V6→V7、重复迁移、问答/消息唯一键、反馈枚举与状态约束、游标索引、反馈引用复合外键和可空 `answer_basis` 回滚兼容，并输出实际迁移版本与约束结果。
- [x] 2.2 新增 `V7__create_web_qa_and_knowledge_gap_tables.sql`，创建 `web_qa_question`、`web_qa_message`、`knowledge_gap_feedback`、`knowledge_gap_feedback_citation` 并向 `agent_run` 追加可空 `answer_basis`；不得修改 V1～V6。
- [x] 2.3 先扩展持久化映射契约测试，再定义问答、消息、反馈、反馈引用实体和 Mapper；所有表名、主键、字段逐项显式注解，机械访问器使用 Lombok，不新增 XML Mapper。
- [ ] 2.4 以 MyBatis-Plus Java API 实现基础仓储；仅对并发幂等插入、复合游标或条件状态更新使用 Mapper 注解 SQL，并用中文注释说明 Java API 无法清楚表达的原因。
- [x] 2.5 运行迁移与映射集成测试并核对数据库实际行、外键、唯一约束、UTC 时间和回滚行为，禁止用 H2 替代 PostgreSQL。

## 3. 补齐回答依据与运行时来源快照

- [x] 3.1 先为新运行持久化 `AnswerBasis`、旧运行按最终引用类型推导、知识来源 metadata 版本化和历史 metadata 缺失降级编写失败测试，测试输出实际 basis、来源类型、范围和时间。
- [x] 3.2 接口优先扩展 `AgentRunSnapshot`、引用安全来源 DTO、证据持久化端口和仓储映射，使用中文 Javadoc 明确“运行当时来源快照”而非当前文档回查。
- [x] 3.3 在运行完成事务中写入 `answer_basis`，并实现旧 `ANSWER` 的知识/代码引用类型推导；无法满足既有引用不变量时返回安全错误，不猜测来源类型。
- [x] 3.4 把知识范围、来源类型、Wiki HTTP(S) URL或原文件名写入有 schema 版本的 `agent_evidence.metadata`，代码索引时间继续使用 `source_updated_at`；不得记录完整整理说明、正文或服务器路径。
- [x] 3.5 运行 Agent 领域、工具、持久化和 Fake Model 回归测试，覆盖知识、代码、混合、冲突、无快照及伪造引用，并检查生产日志与测试证据日志不含问题、URL、答案或证据正文。

## 4. 定义 Web 问答领域与应用契约

- [x] 4.1 定义问答记录、消息角色、Web 可信状态、游标、创建/查询命令与快照，以及 `CreateWebQaQuestionUseCase`、`QueryWebQaQuestionUseCase`、消息投影和仓储端口；公共类型和方法添加中文 Javadoc与稳定失败语义。
- [x] 4.2 先为 `COMPLETED/ANSWER`、冲突拒答、其他拒答、活动状态和失败/终止编写可信状态映射失败测试，中文注释说明防止“失败被展示为可信回答”的回归。
- [x] 4.3 最小实现纯领域映射、问题/幂等键 Unicode 边界和不透明游标编解码，使 4.2 及输入边界测试通过；领域层不得依赖 Spring、HTTP、Mapper 或 Agent 框架。
- [x] 4.4 为“后续问题不继承历史正文”添加应用层失败测试，验证传给 `StartProjectQaRunUseCase` 的问题仅为本次输入，且日志只输出长度与摘要。

## 5. 实现问答创建、幂等历史和终态消息投影

- [x] 5.1 先编写问答应用服务失败测试，覆盖默认 `main`、明确分支、项目/分支无效、相同键重试、同键冲突、并发创建、外层回滚不调度和问答/运行原子可见。
- [x] 5.2 最小实现事务性问答创建：使用客户端键摘要派生 Agent 键，复用 `StartProjectQaRunUseCase`，同事务写入问答与唯一 `USER` 消息，并以 `INSERT ... ON CONFLICT DO NOTHING` 后复读处理并发幂等。
- [x] 5.3 先为历史游标、详情恢复、操作者/项目不匹配和停用项目统一 404 编写失败测试，再实现数据库条件范围查询；不得先跨范围加载后在 Controller 过滤。
- [x] 5.4 先为终态 `ANSWER/REFUSAL` 幂等投影、失败运行不生成助手消息和投影失败后快照自愈编写失败测试，再实现唯一 `ASSISTANT` 消息 materializer；`agent_run` 始终作为终态事实来源。
- [x] 5.5 为问答创建、幂等复用、历史读取、投影完成和失败路径补充结构化日志与测试证据日志，记录 traceId、questionId、runId、项目/分支、状态、数量和错误码，不记录正文。

## 6. 提供问答 REST 契约与统一错误映射

- [x] 6.1 先定义 `POST /api/projects/{identifier}/qa/questions`、历史列表和详情的 HTTP 请求/响应 record、字段校验、分页上限、202/404/409 语义与中文 Javadoc，API DTO 不复用持久化实体。
- [x] 6.2 编写 MockMvc 失败测试，覆盖 ADMIN/MEMBER 成功、匿名 401、输入 400、范围 404、幂等冲突 409、猜测其他记录 404，以及响应不包含 operatorId、内部 generation、对象键或服务器路径。
- [x] 6.3 最小实现问答 Controller、HTTP Mapper 和稳定错误映射，使 6.2 测试通过；Controller 只提取认证身份与映射 DTO，不复制范围、幂等或可信状态规则。
- [x] 6.4 运行平台统一错误、认证、项目详情和问答 Web 契约测试，确认 trace ID、UTC 时间和现有普通接口兼容。

## 7. 实现可续读且有界的 SSE

- [x] 7.1 定义 SSE 公开事件 v1 DTO、事件名映射、`Last-Event-ID`/`afterSequence` 解析、会话存活检查端口和连接配置，明确心跳不占业务序号且原始 Agent/框架对象不得序列化。
- [x] 7.2 先编写 SSE 失败测试，覆盖从零读取、序号 8 后续读、头/参数冲突 400、同序号不重复、严格递增、心跳无 ID、终态关闭、页面刷新快照校正、会话失效和项目停用。
- [x] 7.3 使用 Spring MVC `SseEmitter` 与专用有界调度执行器实现数据库事件轮询、批量发送、心跳、总时限和断开清理；每轮重新校验会话及项目访问权，不把会话标识写入日志。
- [x] 7.4 把终态 SSE 接到同一助手消息 materializer，并确保发送完成后客户端可从详情得到一致正文、拒答/错误码、basis 和引用；发送失败只关闭连接，不改变 Agent 终态。
- [x] 7.5 使用脚本化 Fake Model 与真实 PostgreSQL 完成创建→事件持久化→SSE→断线续读→终态详情集成测试，输出实际首末序号、事件类型、终态、引用数和耗时。

## 8. 实现知识缺口反馈与管理员最小处理

- [ ] 8.1 定义知识缺口类型、`OPEN → ACKNOWLEDGED → CLOSED` 领域状态机、创建/管理用例、仓储端口、过滤游标和稳定错误；中文注释明确反馈不触发知识或索引副作用。
- [ ] 8.2 先编写领域与应用失败测试，覆盖三类反馈、关联问答复制服务端事实、手动问题、范围不匹配 404、输入边界、相同键重试、同键冲突、同状态幂等、跳过/倒退冲突和并发管理员更新。
- [ ] 8.3 最小实现反馈创建事务、请求摘要、引用关联和条件状态更新，使 8.2 测试通过；有关联问答时忽略客户端伪造的问题、结果和引用字段。
- [ ] 8.4 先定义成员创建及管理员列表/详情/状态接口 DTO，再编写并通过 MockMvc 测试，覆盖 MEMBER 可创建但不能访问 `/api/admin/**`、管理员过滤分页、403、404 和 409。
- [ ] 8.5 为反馈创建和状态转换添加生产日志与数据库测试证据，记录反馈 ID、范围、类型、状态前后、引用数与错误码，并断言正式知识、索引和 Agent 运行均未发生业务写入。

## 9. 实现 Pencil 基线的前端问答与反馈体验

- [ ] 9.1 接口优先定义问答、SSE、引用和知识缺口 TypeScript 类型及 `/projects/:identifier/qa` 路由；为路由认证、项目/分支参数和 API 错误转换编写失败测试。
- [ ] 9.2 先为页面级状态 composable 编写失败测试，覆盖加载历史、选择详情、创建幂等键保留、EventSource 序号去重/重连、切换记录关闭旧连接、终态重新拉取快照和主动重试生成新键。
- [ ] 9.3 最小实现类型化 REST/SSE 客户端和页面状态，使 9.2 测试通过；不引入新的状态框架，不把历史问答加入新请求正文。
- [ ] 9.4 先为最近问题侧栏、范围锁定栏、消息/阶段区、三种可信状态、输入区和引用抽屉编写组件行为测试，再按 Pencil `04 · 项目问答 / 引用展开` 与 UI Kit 实现真实数据页面。
- [ ] 9.5 实现“业务设计原因 / 当前快照实现”分区和安全来源卡片；答案与文件名按纯文本渲染，Wiki 链接只允许 HTTP(S) 并带 `noopener noreferrer`，代码来源展示相对路径、分支、commit 和索引时间。
- [ ] 9.6 先为拒答、冲突、无快照、模型不可用、SSE 中断和空历史编写前端失败测试，再实现明确降级、重连和普通文档/搜索入口，禁止把阶段或部分内容标成可信完成。
- [ ] 9.7 先为反馈类型推荐、范围确认、提交防重、网络不确定时复用原键和成功确认编写测试，再实现知识缺口对话框；反馈失败时保留用户说明。
- [ ] 9.8 完成键盘提交、可信状态文字、引用抽屉焦点恢复、表单标签和错误关联检查；运行组件测试确认状态不只依赖颜色且桌面布局无溢出。

## 10. 首个可用闭环验证与交付门禁

- [ ] 10.1 运行全部后端单元测试、Web 契约测试和真实 PostgreSQL 集成测试，确认 T1～T6A 回归、V7、问答、SSE、反馈与敏感字段断言全部通过，并保存实际测试证据输出。
- [ ] 10.2 运行前端类型检查、单元/组件测试和生产构建，确认现有登录、项目、知识页面未回归且问答路由可直接刷新。
- [ ] 10.3 在无真实公司材料的本地环境使用两个项目、两个分支、已发布模拟知识、活动模拟代码快照和 Fake Model 完成“提问→流式阶段→可信回答/拒答→引用→刷新恢复→知识缺口”的浏览器闭环检查。
- [ ] 10.4 验证模型禁用和模型不可用时 readiness、文档浏览、知识搜索和代码搜索仍可用；检查日志不含完整问题、答案、反馈说明、Wiki URL、Token、内部地址、对象键或绝对路径。
- [ ] 10.5 核对实现与 proposal/specs/design 一致，更新必要的 API/运行说明；仅在全部完成标准满足后勾选本文件实际任务、同步主规格并把 `docs/product/LoreDock_MVP功能开发计划.md` 的 T7 更新为 `[x]`，不得提前标记 T6B、T8～T13。

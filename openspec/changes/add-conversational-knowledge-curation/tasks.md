## 1. 契约与失败测试

- [ ] 1.1 定义知识任务会话、消息、run 关联、暂停/恢复、草稿修订、结构化更新、Diff 和发布修订锁定契约，并补充中文 Javadoc
- [ ] 1.2 增加 Flyway 与 PostgreSQL 失败测试，验证触发幂等、会话隔离、修订递增、基础修订冲突、Tool 重试和发布冲突
- [ ] 1.3 增加 Agent/Checkpoint 失败测试，验证系统首轮、先读后改、安全暂停、指导恢复、完成后新 run 和重启恢复
- [ ] 1.4 增加前端失败测试，验证对话/事件、暂停状态、完成后输入、修订历史、Markdown Diff 和发布前冲突

## 2. T6B 平台最小实现

- [ ] 2.1 实现知识任务会话与消息 Service，使手动和定时触发复用同一入口且每次执行保留独立 run
- [ ] 2.2 将 PostgresSaver、稳定 threadId 和启动恢复接入知识长任务，完成 `PAUSE_REQUESTED → WAITING_FOR_USER → RUNNING` 状态机
- [ ] 2.3 实现 `draft_create`、`draft_read`、`draft_update` 和 `draft_diff` 契约及安全 Tool 注册
- [ ] 2.4 实现草稿区块、乐观修订、Tool 幂等、来源关联、不可变修订与服务端 Diff
- [ ] 2.5 将发布操作锁定到管理员已查看的明确草稿修订，保持 Agent 无正式发布权限

## 3. T8 知识整理接入

- [ ] 3.1 编写本地 `knowledge_curator` Skill 和预定义子 Agent Spec，要求读取当前修订后按审核意义批次增量更新
- [ ] 3.2 实现系统/手动首轮、用户暂停指导、证据不足等待和完成后继续调整的协调 Agent 接入
- [ ] 3.3 将检索、冲突候选、来源、草稿修订和公开运行事件关联到同一任务会话

## 4. 前端与验证

- [ ] 4.1 实现知识任务对话与产物双栏、过程折叠、暂停/恢复、完成后输入和修订列表
- [ ] 4.2 实现正式/空基线到当前修订及任意修订间的 Markdown Diff 审批，处理截断和修订冲突
- [ ] 4.3 同步 API、数据模型和 Pencil 增量设计，明确对话消息不等于草稿产物
- [ ] 4.4 运行后端单元/契约/PostgreSQL 集成测试、前端测试与构建、OpenSpec 严格校验，并检查中文注释和敏感信息

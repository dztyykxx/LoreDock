# LoreDock 项目协作规范

## 1. 适用范围

本文件适用于 LoreDock 仓库中的所有人员和编码 Agent，是项目开发约束的唯一权威来源。

除非用户在当前任务中明确提出例外，否则所有需求分析、规格设计、代码实现、测试和文档修改都必须遵守本文件。

### 1.1 当前快速迭代阶段优先规则

项目当前处于核心流程快速迭代阶段。本节规则优先于本文后续关于 OpenSpec、边界完整性、测试范围、日志脱敏和完成门禁的常规要求，直到用户明确宣布退出快速迭代阶段：

- 第一优先级是尽快跑通知识导入与索引、检索、项目问答、证据引用和前端展示等核心链路；实现与修复必须围绕可运行的端到端主流程，不为尚未影响主流程的边界提前设计；
- Bug 和核心能力改动可以直接定位、修改、验证，不要求先创建或更新 OpenSpec change，也不为细小问题拆分大量 proposal、spec、design 或 task；只有用户明确要求时才执行 OpenSpec 流程；
- 测试采用最小且有代表性的集合，优先验证真实核心路径和本次修复；非核心边界、穷举组合、完整兼容性和大范围回归可以留待后续重构阶段；
- 非核心模块以基本可用、不阻塞主流程为目标，不为了形式完整增加抽象、扩展点、复杂降级或防御性分支；
- 遇到现有设计复杂、重复或边界过多时，当前阶段先做最小修复，不顺带开展大范围重构；后续统一重构简化；
- 交付判断以核心流程是否真实可运行、关键结果是否可观察为主，不因未补齐非核心工件或低优先级边界而阻塞交付。

## 2. 项目定位

LoreDock 是面向开发团队的项目业务上下文知识平台。系统集中管理公司 Wiki、项目文档、通用业务知识、需求材料、PR/Commit、测试证据和轻量代码快照，通过 Web 问答和 MCP 为开发人员及其本地 Agent 提供可检索、可引用的业务上下文。

系统的职责边界是：

- 服务端知识库负责提供业务含义、设计原因、约束、历史经验和可追溯证据；
- Web 问答可以参考指定项目、分支和 Commit 的服务端代码快照；
- 本地 Agent 以用户本地工作区中的最新代码为实现事实，LoreDock 不替代本地代码阅读；
- Agent 可以生成知识草稿，但不得绕过人工审核直接发布正式知识。

## 3. 文档与事实来源

开始工作前，应按任务需要阅读以下资料：

- 当前需求基线：`docs/product/项目业务上下文知识库_MVP需求文档_v1.0.md`
- MVP 功能开发计划：`docs/product/LoreDock_MVP功能开发计划.md`，用于确认任务顺序、依赖、范围、状态和完成门禁
- 项目选题与演示背景：`docs/product/项目业务上下文知识库_MVP_选题说明.md`
- Java 技术栈建议：`docs/research/Java技术栈调研与MVP落地建议.md`
- 开源项目调研：`docs/research/开源代码知识库项目调研_v0.1.md`
- UI 设计基线：`docs/UI/README.md`，Pencil 源稿位于 `docs/UI/LoreDock-MVP-UI.pen`，总览预览位于 `docs/UI/LoreDock-design-overview.png`
- 历史需求：`docs/archive/requirements/`，仅用于追溯，不作为当前实现依据

当信息冲突时，优先级从高到低为：

1. 用户在当前任务中的明确要求；
2. 已确认且仍处于有效状态的 OpenSpec 规格；
3. 当前需求基线文档；
4. 技术调研与选题说明；
5. 历史归档文档。

不要把调研建议误当作已经确认的需求。需要改变需求、接口、数据模型或关键技术决策时，先更新 OpenSpec 工件。

开始实现具体功能前，Agent 必须在 MVP 功能开发计划中定位对应任务，并确认其前置依赖。开发计划不替代需求基线和 OpenSpec；任务完成后应同步更新计划中的任务状态，避免后续 Agent 依据过期信息工作。

涉及 Web 页面、交互状态或视觉实现时，Agent 必须先阅读 `docs/UI/README.md` 并在 Pencil 中查看 `docs/UI/LoreDock-MVP-UI.pen`。`.pen` 是 Pencil 加密文件，只能通过 Pencil 或 Pencil MCP 工具访问，禁止使用普通文本读取、搜索或改写工具处理。UI 设计稿用于约束页面结构、信息层级、状态表达和视觉规范，不得覆盖更高优先级的需求基线或 OpenSpec；发现冲突时应按本节事实优先级处理并同步设计说明。

## 4. SDD：规格驱动开发

项目采用 SDD（Specification-Driven Development）。凡是会改变外部行为、业务规则、接口契约、数据结构、持久化方式、检索结果、Agent 工具行为或系统集成的改动，都必须先建立或更新 OpenSpec change，再开始实现。

标准流程：

1. 明确问题、目标、范围和非目标；
2. 创建 OpenSpec change；
3. 完成 `proposal`、增量 `specs`、必要的 `design` 和 `tasks`；
4. 确认规格可验收、边界清楚后再进入实现；
5. 按任务顺序使用 TDD 完成实现；
6. 验证通过后同步主规格并归档 change。

规格要求：

- 使用可观察、可验证的行为描述，不使用“支持一下”“优化体验”等模糊表述；
- 每条关键需求至少包含正常场景、重要边界和明确失败行为；
- 涉及知识检索时，说明项目、分支、知识范围、引用和拒答条件；
- 涉及接口时，说明请求、响应、错误语义、幂等性和兼容性；
- 涉及后台任务时，说明状态变化、失败恢复和重复执行行为；
- 明确非目标，防止在实现阶段扩大范围。

纯文字纠错、格式调整以及不改变行为的机械性修改，可以不单独创建 OpenSpec change。无法判断是否改变行为时，按需要创建 change 处理。

OpenSpec 是决策记录工具，不是所有修改的强制仪式。必须使用 OpenSpec 的情况：新增或改变外部行为、接口契约、数据结构或持久化规则、关键业务规则、模块职责或依赖关系，引入重要技术决策，或需要多步骤实施并长期追踪的变更。可以直接修改并验证、不需要 OpenSpec 的情况：小型 Bug 修复、机械移动或重命名、不改变行为的代码简化、注释格式和文档纠错、删除确认无用且无外部影响的代码、可以直接说明和验证的局部调整。

规格必须先写核心场景、非目标、删除项和复杂度边界。不得因为“以后可能需要”引入当前没有入口的版本、发布、回滚、审计和扩展点；发现规格方向错误时先修正规格，不继续用 TDD 固化错误架构。

需求和 OpenSpec 范围确定前必须先完成“现有能力还是自定义实现”的判断，不能等到编码阶段才检查框架能否复用：

- 对运行时、工作流、Agent、多 Agent、任务调度、持久化、认证等大型能力，proposal 或需求分析必须列出“项目已引入框架的原生能力、LoreDock 需要补充的业务能力、确需自定义的缺口”；
- 先核对项目锁定版本的本地依赖源码/API，再以官方文档、官方仓库和发行说明确认能力与限制；不得依据类名猜测，也不得把框架已提供的能力写成 LoreDock 自研需求；
- 框架能够满足核心场景时，需求和 design 必须明确直接使用的框架组件，实施任务只保留配置、薄适配、业务 Tool、权限范围、幂等和业务持久化；
- 只有确认框架在当前锁定版本中无法满足已验收行为时，才允许把自定义运行时或基础设施写入规格，并记录缺口、替代方案和验证依据。

## 5. TDD：测试驱动开发

项目采用 TDD（Test-Driven Development）。业务行为的实现遵循“红—绿—重构”：

1. 根据已确认规格选择一个有业务意义的行为；
2. 先编写能够表达该行为且当前会失败的测试；
3. 确认测试因缺少目标行为而失败，而不是因为测试本身错误；
4. 编写满足该行为的最小实现；
5. 运行相关测试并使其通过；
6. 在测试保护下进行必要重构；
7. 再处理下一个行为。

测试数量不是目标。禁止为了覆盖率、测试数量或形式完整而堆砌测试。

测试选择原则：

- 优先覆盖业务规则、权限或范围隔离、状态转换、错误恢复、版本/分支差异、引用和拒答等高风险行为；
- 一个测试应说明一个清晰的业务事实；多个输入只有在表达同一业务规则时才适合参数化；
- 不测试框架自身、无业务逻辑的 getter/setter、常量赋值或纯粹实现细节；
- 单元测试用于稳定的领域规则，集成测试用于数据库、检索、事务、接口契约和外部适配边界；
- 优先使用最小且有代表性的测试集，不重复验证同一行为；
- 不允许通过删除断言、放宽断言或无理由跳过测试来换取通过。

每个测试用例都必须带有中文注释，明确说明：

- 该测试保护的业务目的；
- 为什么这个场景值得测试，或者它防止哪类回归。

测试方法名和其他代码标识符一样必须使用英文，并清楚表达“条件—行为—结果”；中文业务目的写在测试注释中，注释不能只把方法名换一种说法。

## 6. 后端接口优先

后端开发必须先定义契约，再编写实现，顺序如下：

1. 在 OpenSpec 中确认接口行为和验收场景；
2. 定义请求/响应模型、领域端口或 Java 接口；
3. 为契约和业务行为编写失败测试；
4. 编写实现类；
5. 接入 Controller、持久化或外部适配器；
6. 完成集成验证。

不得先写实现类，再根据实现反推接口。接口应表达稳定的业务能力，而不是简单复制实现细节。不要为了“接口优先”给没有替换边界、没有业务语义的类机械增加接口。

## 7. 后端包结构与依赖方向

- 后端采用“按业务能力分包、模块内部简单 MVC”的结构，不采用全局横向 `controller/service/mapper` 目录，也不使用 DDD、六边形或整洁架构；
- 业务模块为 `project`、`knowledge`、`code`、`agent`、`qa`、`job`、`feedback`、`auth`、`storage`；模块内部使用 `Controller → Service → Mapper → PostgreSQL` 简单 MVC；
- `Controller` 只处理 HTTP 协议、必要参数校验、认证上下文和响应转换；`Service` 集中业务流程、规则、事务和模型转换；`Mapper` 只保存 MyBatis-Plus 数据访问接口；Service 可以直接使用本模块 Mapper，Controller 和 Service 不得直接访问其他模块 Mapper；
- `platform` 只保存跨能力基础约定：Web 输入能力放在 `platform.web`，时间、审计和持久化公共支持分别放在语义明确的子包；不得把无法归类的业务代码堆入 `common`、`util` 或 `platform`；
- 禁止创建 `domain`、`application`、`infrastructure` 分层以及 `Repository`、`UseCase`、`Port`、`Gateway`、`Coordinator`、`Facade`、`Adapter` 等未批准结构；不再为 Mapper 套 Repository 层；
- `model` 子包按需创建：`model/entity` 只保存数据库实体，`model/enums` 保存模块内枚举，`model/dto` 统一保存 Request、Response 等 HTTP 载体和确有必要的数据载体；不默认创建 `command`、`result`、`snapshot`、`converter` 等子包，已存在的同类小类按模块重构时合并或删除；
- 跨模块只能引用对方模块的 `api` 契约包：`api` 只保存 Service 接口、接口必需的最小不可变输入/输出类型和少量稳定枚举，不放 Controller、Service 实现、Mapper、Entity、配置、调度器或内部过程模型；
- 接口命名直接使用 `XxxService`，实现类使用 `XxxServiceImpl`，不使用 `IProjectService` 风格；只有确实被其他模块调用的模块才创建 `api` 包，没有跨模块调用的模块不创建空 `api`；
- 模块内部、单一调用且没有替换或边界价值的简单 Service 直接使用具体类，实现数量明显增加后再拆 `service/impl`；HTTP DTO 只有在语义完全相同时才复用 `api` 契约类型，包含展示、分页、JSON 兼容字段时仍放在 `model/dto`；
- 模型创建遵循最小原则：Entity 只表达数据库映射，不作为 HTTP 请求/响应；Request/Response 只在接口存在独立契约时创建；不为同一组字段机械创建 Entity、DO、PO、DTO、BO、VO、Command、Result 多套对象；单个方法内部临时数据优先使用私有 `record`；
- 模块依赖保持单向、无环：`Qa → Agent`、`Agent → Project/Knowledge/Code`、`Knowledge/Code → Project`、`Knowledge/Code → Job/Storage`；跨模块依赖指向对方 `api`，禁止跨模块引用对方 `service`、`mapper`、`model/entity`、内部 DTO、Snapshot、Command、Result 或实现异常；
- 命名习惯：查询方法使用 `get`、`list`、`page`、`exists`，写操作使用 `create`、`update`、`delete`、`publish`、`start`；`get` 表示期望存在（不存在通常抛业务异常），`find` 表示允许不存在；类名不使用含义模糊的 `Manager`、`Helper`、`Processor`。

## 8. 注释规范

代码标识符使用英文，面向项目成员的文档和代码注释使用中文。

后端接口及其实现必须包含必要注释：

- 对外 API、Java 公共接口、公共类和公共方法使用中文 Javadoc，说明职责、关键参数、返回结果和重要失败语义；
- 实现类的类注释说明采用的实现策略、事务边界或主要约束；
- 关键业务分支必须说明“为什么这样处理”“对应什么业务规则或兼容要求”；
- 能关联规格时，在注释中引用 OpenSpec capability、需求编号或业务术语；
- 不在注释中记录密钥、内部地址或敏感数据。

禁止低价值注释：

- 不逐行复述代码；
- 不写“设置变量”“调用方法”一类显而易见的说明；
- 不复制接口 Javadoc 充当实现说明；
- 不保留已经与代码不一致的历史注释。

注释的目标是保存代码本身无法表达的业务原因、约束和风险。

## 9. 日志与可验证性规范

后端关键业务逻辑必须提供能够证明真实执行过程的结构化日志。关键业务逻辑包括用例入口、范围解析、权限或状态判断、后台任务、事务状态转换、索引或检索、失败恢复以及外部适配边界。

- 关键流程至少在开始、重要业务决策或状态转换、完成和失败位置记录日志；不得只输出“执行成功”而缺少实际业务上下文；
- 日志字段按场景记录 `traceId`、操作名、项目/分支/实体/任务标识、变更前后状态、处理数量、耗时、结果和错误码等关键信息，使维护者能够确认执行范围、路径和结果；
- 正常生命周期使用 `INFO`，可恢复或预期异常使用 `WARN`，非预期失败使用 `ERROR`；异常日志必须保留脱敏后的业务上下文和异常语义；
- 日志不得记录密码、Token、连接串、内部绝对路径、原始对象键、私钥、完整知识正文或代码正文。可能敏感的查询和输入只记录长度、数量、稳定标识或脱敏摘要；
- 避免在循环中逐条输出相同日志或记录无业务意义的框架噪声，日志应服务于行为核验、故障定位和审计追踪。

### 核心流程快速开发阶段的 stdout 调试规则

在当前快速迭代阶段，以下规则优先于本节关于脱敏、正文、向量和循环日志的限制：

- 知识检索、代码检索和项目问答必须使用 `System.out.println` 直接输出调试数据，不通过 SLF4J、Logback 或其他日志包装，确保控制台内容没有日志框架前缀；
- stdout 只保留用户原始问题、每次 Agent 工具调用的结果数量与代表性内容预览、模型最终原始响应；工具结果中的知识或代码正文最多展示前 500 个 Unicode 码点；
- 不输出查询向量、完整候选列表、固定范围对象、完整 Prompt、每个流式分片、重复的解析对象或同一工具结果的多份副本；全量中间数据会淹没有效信息；
- 新增或修改检索、Agent、问答核心链路时，应维持上述三类 stdout 输出，确保一次运行可以直接看清“用户问了什么、工具找到了什么、模型最终回答了什么”。

后端业务测试也必须输出可核验的关键证据，不能仅以测试框架的 `passed` 作为实现已真实执行的依据。

- 每个保护业务行为的测试用例必须在调用真实目标逻辑后，记录场景名称、业务范围以及断言对应的实际关键结果，例如状态变化、命中范围、处理数量、错误码或耗时；
- 集成测试必须记录实际经过的基础设施边界和最终结果，例如数据库类型与迁移版本、后台任务终态、索引文档数或查询命中范围，但不得暴露连接信息、服务器路径或业务正文；
- 测试证据日志必须来自被测代码的实际返回值或可观察状态，并与断言一致；禁止预先打印固定的“成功”信息、伪造执行结果或用日志代替断言；
- 修改关键业务逻辑时，必须同步检查并补充对应生产日志和测试证据日志。无法从日志确认被测范围、关键路径与实际结果的关键业务实现，不视为完成。

## 10. 后端框架与持久化约束

- 后端业务数据库访问统一使用 MyBatis-Plus；除非有效 OpenSpec 明确记录例外，不得另写一套 JDBC、JPA 或其他 ORM 持久化实现；
- Flyway 是数据库结构变更的唯一入口。MyBatis-Plus 只负责数据映射和访问，不得自动创建、更新或删除表结构；集成测试必须使用与生产兼容的真实 PostgreSQL，不得用 H2 替代验证；
- 持久化实体必须与领域对象、API DTO 分离。每个持久化实体使用 `@TableName` 显式声明表名，主键使用 `@TableId`，其余每个持久化字段都使用 `@TableField` 显式声明列名，不得依赖驼峰转换等隐式映射；
- SQL 优先使用 MyBatis-Plus 的 `BaseMapper`、Lambda Wrapper 等 Java API 实现；Java API 无法清楚表达时才使用 Mapper 方法上的 `@Select`、`@Update` 等注解 SQL；禁止使用 XML Mapper；
- 持久化实体和简单 Java 数据载体优先使用 Lombok 的 `@Getter`、`@Setter`、`@Builder`、构造器等注解，避免手写机械 getter/setter；不可变 DTO、值对象和需要保护不变量的领域模型可以使用 Java `record` 或显式方法，不得为了使用 Lombok 破坏不可变语义；
- 用户认证统一使用 Sa-Token，不自行重复实现会话、登录状态或权限框架；具体登录与权限规则仍须先在对应 OpenSpec 中确认；
- 密码只保存带盐的 BCrypt 单向哈希，不保存明文，不使用可逆加密，也不得使用裸 MD5、SHA-1 或 SHA-256 直接代替密码哈希。认证属于非核心基础能力时保持实现简单，不额外引入未被规格要求的复杂认证系统。

## 11. 能力复用与依赖引入

- 能力复用判断属于需求和规格确定的一部分，不是实现阶段的可选优化；大型功能没有完成框架能力对照前，不得冻结需求范围或进入 TDD；
- 实现功能前必须按“JDK 标准库 → 项目已引入的框架或依赖 → Hutool → 成熟且持续维护的第三方库 → 自定义实现”的顺序检查现有能力；框架已经提供可靠实现时不得重复造轮子；
- 优先使用 Spring Boot Starter、自动配置和依赖管理，以及项目已经选定的 Spring、MyBatis-Plus、Sa-Token、Flyway、Jackson、Lombok 等官方能力。不得为了统一外观对框架 API 进行无业务价值的二次封装；
- 允许并鼓励在实现前通过互联网调研已有工具或框架。调研优先查看官方文档、官方仓库、发行说明、Maven Central 和安全公告，不以未经核验的博客或代码片段作为关键选型依据；
- Hutool 是个人技术栈默认工具库；当前项目尚未引入，确有真实需求时按本节规则引入最小模块。Apache Commons、Guava、Caffeine、Resilience4j 等仍是候选，必须结合实际需求比较现有依赖和 JDK 能力，不得因为调用方便就引入功能大幅重叠的库；
- 新增第三方依赖前必须确认其解决了当前任务中的真实问题，并检查维护状态、许可证、已知安全风险、Java 21/Spring Boot 4 兼容性、传递依赖和体积影响；只使用少量简单方法且 JDK 可以清楚实现时，不应为此增加大型依赖；
- 新依赖应尽量按需引入最小模块，版本必须由 Spring Boot 依赖管理或项目版本矩阵明确锁定；同一能力不得同时保留多套实现。涉及外部行为、数据结构或关键技术决策时，必须先更新 OpenSpec；
- 认证、会话、密码哈希、权限、序列化、参数校验、数据库迁移、事务、重试、缓存、日志、HTTP 客户端和常用 IO 等通用技术能力，优先采用项目框架或成熟库，禁止自行实现安全协议、加密算法或通用基础框架；
- 只有在现有能力确实无法满足已确认规格时才允许自定义实现，并在设计或代码注释中说明缺口、选择原因、边界和验证方式。领域状态机、范围隔离、引用/拒答等 LoreDock 特有业务规则仍应由项目代码明确表达，不得强行套入通用工具库；
- 对外部系统或可替换基础设施可以保留必要端口和适配器，但抽象必须对应真实业务边界、测试边界或替换需求，不为单一调用机械创建接口、包装器或 `util/common` 层。

### 11.1 Spring AI Alibaba Agent 能力复用

- Agent 相关需求必须优先直接使用项目锁定版本的 Spring AI Alibaba Agent Framework 与 Graph：`ReactAgent`、`FileSystemSkillRegistry`、`SkillsAgentHook`、`SkillsInterceptor`、`AgentSpecLoader`、`AgentSpecReactAgentFactory`、`TaskToolsBuilder`、`TaskTool`、`AgentTool`、框架 Hook/Interceptor、`ToolCallback`/`ToolCallbackResolver`、`PostgresSaver`、`RunnableConfig.threadId` 和 Human-in-the-Loop；
- LoreDock 的职责是实现知识搜索、文档读取、证据读取、草稿更新、Diff、报告和冲突候选等业务 Tool，以及 Tool 内的身份、项目范围、知识状态、参数上限、来源、幂等和正式发布边界；
- 禁止重复实现通用 Agent Runtime、Skill Registry/Loader、Agent Spec Loader、子 Agent 调度器、Tool Registry 框架、Graph Checkpoint、模型/Tool 循环或 Human-in-the-Loop 机制；只允许为框架缺失的明确业务语义增加薄适配；
- Skill 和 Agent Spec 只获得服务端显式提供的安全 `ToolCallback`；不得向框架注册 Shell、任意 HTTP、数据库管理、任意文件系统或正式知识发布 Tool。框架默认示例中存在的高权限 Tool 不代表 LoreDock 获得使用许可；
- 若框架默认行为与 LoreDock 安全要求存在差异，例如 Agent Spec 未声明 Tool 时默认取得全部候选 Tool、未知 Tool 被忽略或无效文件只记录告警，项目只增加运行前的确定性校验，不重写框架加载和调度实现。

## 12. 实现原则

- 只实现已确认规格和当前任务需要的内容，不提前建设未来功能；
- 优先采用简单、可维护、可验证的方案，不为单一场景创建多余抽象；
- 业务规则应集中在模块 Service 中，不散落在 Controller、Mapper 和平台代码里；
- Web、内部服务和 MCP 应复用同一套权限、范围与检索逻辑，禁止复制出多套实现；
- 项目、分支、Commit 和知识范围是检索的强约束，禁止先跨范围召回再在展示层隐藏；
- 回答必须带来源；证据不足时明确拒答，不允许使用模型常识补造内部事实；
- 捕获异常时必须保留语义和上下文，不得静默吞掉异常；
- 不提交凭据、Token、内部真实代码、公司 Wiki 原文或其他敏感数据到公开仓库。

### 12.1 决策权与 AI 协作边界

项目方向由开发者决定：项目解决什么问题、哪条用户流程最重要、MVP 做什么不做什么、模块如何划分、数据归谁拥有、模块间接口、当前值得承担的复杂度。AI 在已确认边界内负责实现、测试、重构、更新相关文档和检查依赖与复杂度，不得替开发者决定产品和架构方向。

AI 必须暂停并确认的变更：新增产品能力；改变核心流程；新增模块或改变模块职责；改变模块间接口；存在多个会显著影响后续开发的方案；引入新的大型依赖或基础设施；增加版本、发布、恢复、审计或扩展体系；复杂度明显超过任务预算；需要修改大量无关模块。

AI 不得执行：根据生产级最佳实践自动扩张 MVP；在没有当前入口时建设未来能力；用更多抽象掩盖不清晰的职责；先实现再反推需求和接口；用 TDD 固化未经确认的规格；为通过测试删除断言、吞掉异常或隐藏真实问题；连续完成大量任务而不留人工审查点。

### 12.2 复杂度控制信号

出现以下信号必须暂停并说明正在解决哪个当前问题，优先按“删除 → 合并 → 字段化/JSON 化 → 直接使用框架 → 最后才新增抽象”处理：

- 一个普通功能新增超过一张业务表；
- 核心链路尚未跑通就新增超过十个生产类；
- 新增超过两个非 Mapper 公共接口；
- 为单一实现创建 Interface、Impl、Port、Adapter 多层结构，或为一组字段创建多层 DTO 转换；
- 一个公共类只有几行且只转发一次调用；
- Service 直接访问其他模块 Mapper，或多个模块直接依赖同一个持久化 Entity；
- 模块之间出现循环依赖；
- 为尚无入口的能力增加版本、发布、恢复、缓存、重试或兼容层；
- 同一业务事实在多个文档或表中重复保存；
- 框架已有能力，项目又自行实现或包装一套。

## 13. 完成标准

一项功能只有同时满足以下条件才算完成：

- 对应 OpenSpec 工件已经完成并与实现一致；
- 接口或契约先于实现定义；
- 核心路径经过真实基础设施或真实模型验证，可观察到实际结果，不只是一次编译或 Mock 通过；
- 关键业务行为有真正有意义的测试保护；
- 每个测试用例都有说明业务目的的中文注释；
- 后端接口、实现及关键业务逻辑具备符合本规范的中文注释；
- 后端关键业务流程具备可确认执行范围、状态转换、结果和失败语义的结构化日志；
- 业务测试输出与断言一致的实际执行证据，能够确认被测逻辑、业务范围和关键结果；
- 相关测试通过，并检查了受影响范围；
- 没有通过降低断言质量、隐藏异常或扩大范围来完成任务；
- 没有规格之外的未来能力，没有可以删除或合并的低价值类和表；
- OpenSpec `tasks.md` 中完成的任务已及时勾选；
- 必要的规格、使用说明和技术文档已经同步。

## 14. Git 提交规范

- 提交前必须检查工作区差异和待提交文件，只纳入当前任务相关改动，并确认未包含凭据、Token、构建产物或其他敏感数据；
- Git 提交标题采用 `<type>(<scope>): <中文简述>` 格式，`scope` 可省略；类型和范围使用英文小写，中文简述应准确概括实际改动，例如 `feat(search): 增加知识混合检索`；
- `type` 必须从以下类型中选择：`init` 表示项目或模块初始化，`feat` 表示新增功能，`fix` 表示缺陷修复，`refactor` 表示不改变外部行为的重构，`perf` 表示性能优化，`test` 表示测试改动，`docs` 表示文档或规范改动，`build` 表示构建或依赖改动，`ci` 表示持续集成改动，`chore` 表示其他不改变产品行为的维护任务，`revert` 表示回退已有提交；
- 禁止使用“更新代码”“修复问题”等无法说明范围的模糊标题；破坏性变更应在类型或范围后增加 `!`，并在正文说明迁移影响；
- 每个提交都必须在正文中使用“改动点：”简要列出至少一项实际改动，并使用“验证：”记录已执行的测试或检查；未运行测试时必须明确写明原因，不得伪造验证结果；
- 提交信息示例：

  ```text
  docs: 完善 Git 提交规范

  改动点：
  - 统一提交标题的类型前缀与可选范围
  - 要求提交正文记录实际改动和验证结果

  验证：
  - 检查 AGENTS.md 格式与示例
  ```
- 一个提交应对应一个清晰、可审查的逻辑改动，正文只说明关键改动和验证证据，不堆砌实现过程；
- 提交前应运行与改动风险相匹配的测试或检查；若存在未执行的验证，必须在交付说明中明确指出。

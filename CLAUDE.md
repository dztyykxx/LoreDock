# LoreDock Claude Code 协作说明

## 必读规则

开始任何分析、规格编写或代码修改前，必须先阅读仓库根目录的 `AGENTS.md`。`AGENTS.md` 是本项目对所有编码 Agent 的权威规则，本文件仅补充 Claude Code 的执行方式。

如果本文件与 `AGENTS.md` 不一致，以 `AGENTS.md` 为准；如果用户在当前任务中明确提出不同要求，以用户当前要求为准，并指出由此产生的规格或实现影响。

## OpenSpec 工作流

项目采用 SDD + TDD。涉及业务行为、接口、数据模型、检索、MCP 或集成的改动，不得直接开始编码。

Claude Code 应使用仓库中的 OpenSpec 命令和技能：

- `/opsx:explore`：澄清问题和方案，不直接实现；
- `/opsx:propose`：创建 change，并生成 proposal、specs、design 和 tasks；
- `/opsx:update`：实现中发现规格问题时更新工件；
- `/opsx:apply`：读取完整 change 后按 tasks 实现；
- `/opsx:sync`：将增量规格同步到主规格；
- `/opsx:archive`：验证完成后归档 change。

在实现前必须确认：

1. 当前 change 已选定；
2. OpenSpec 要求的工件完整；
3. 需求场景可以被测试验证；
4. tasks 已按“契约/接口—失败测试—实现—验证”的顺序拆分。

如果实现暴露出规格缺失或设计冲突，停止继续扩展实现，先更新 OpenSpec 工件。

## 开发执行要求

- 后端先定义 API 契约、DTO、领域端口或 Java 接口，再编写实现类；
- 对有业务意义的行为严格执行“先写失败测试，再写最小实现，再重构”；
- 每个测试用例必须有中文注释，说明业务目的和防止的回归；
- 测试数量和覆盖率不是目标，不编写没有业务价值的测试；
- 后端公共接口、公共类、公共方法以及实现类必须有必要的中文 Javadoc；
- 关键业务逻辑的注释必须解释原因、规则和风险，不能逐行翻译代码；
- 只修改当前任务需要的文件，不顺手重构无关代码；
- Web、服务层和 MCP 复用同一业务逻辑，尤其是项目/分支范围、权限、引用和拒答规则；
- 不得把公司内部代码、Wiki 原文、凭据或真实业务数据提交到公开仓库。

## 每次实现前的检查清单

- 已阅读 `AGENTS.md` 和当前 OpenSpec change 的全部上下文文件；
- 已确认当前需求基线和非目标；
- 已先定义接口或契约；
- 已选择最少但有代表性的业务测试；
- 已为测试和关键业务逻辑规划中文注释；
- 已明确本次改动的验证命令和完成条件。

## 每次实现后的检查清单

- 相关测试全部通过；
- 测试失败与通过分别验证了目标业务行为；
- 没有无意义、重复或只为覆盖率存在的测试；
- 接口、实现和关键业务规则注释完整且没有过期；
- 实现与 OpenSpec 一致，任务复选框已同步；
- 最终说明包含变更结果、验证结果和仍然存在的限制。

